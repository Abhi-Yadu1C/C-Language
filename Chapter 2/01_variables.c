#include <stdio.h>

int main(){

    // Type declaration Instruction

    int i = 10; // Declare and initialize 'i' with 10
    int j = i;
    int a = 2, b = 3, c = 4, d = 5;

    // %d is called formate specifier
    // %d is for int, %f is for flot and %c is for char

    printf("The value of i is %d and value of j is %d \n", i, j );
    printf("The value of i is %d and value of j is %d \n", a, b);
    printf("The value of i is %d and value of j is %d \n", c, d);


    return 0;
}